rsa:
  public-key: classpath:certs/public.pem
  private-key: classpath:certs/private.pem
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    url: jdbc:postgresql://localhost:5432/auth
    password: casablanca3
  jpa:
    show-sql: 'true'
    hibernate:
      ddl-auto: create